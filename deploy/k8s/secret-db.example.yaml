# Copy to secret-db.yaml (gitignored), set a strong password, then: kubectl apply -f secret-db.yaml
apiVersion: v1
kind: Secret
metadata:
  name: hirekaar-db-secret
  namespace: hirekaar
type: Opaque
stringData:
  password: "change-me-strong-password"
