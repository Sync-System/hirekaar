# Next.js frontend — optional copy: cp .env.example .env
# Repo root `.env` is loaded first; this file overrides when present (see next.config.ts).
#
# Server-only: FastAPI base (no trailing slash). Same host as https://hirekaar-backend.vercel.app/
API_BASE_URL=https://hirekaar-backend.vercel.app

# Public URL of this Next app (browser)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
